<script setup lang="ts">import{ref}from'vue';defineProps<{open:boolean}>();const emit=defineEmits<{close:[];unlock:[password:string]}>();const password=ref('')</script>
<template><div v-if="open" class="backdrop" role="dialog" aria-modal="true"><section class="dialog"><h2>解锁本地凭据</h2><p>输入应用主密码以使用已保存的 SSH 密码和私钥口令。</p><input v-model="password" type="password" autocomplete="current-password" aria-label="应用主密码"/><footer><button @click="emit('close')">取消</button><button class="primary" :disabled="!password" @click="emit('unlock',password)">解锁</button></footer></section></div></template>
<style scoped>.backdrop{position:fixed;inset:0;display:grid;place-items:center;background:#0008;z-index:10}.dialog{width:min(400px,calc(100vw - 32px));border:1px solid var(--line);border-radius:10px;background:var(--surface);padding:20px}.dialog h2{margin:0;font-size:16px}.dialog p{color:var(--muted);font-size:13px}.dialog input{width:100%;border:1px solid var(--line);border-radius:6px;background:var(--canvas);padding:8px;color:var(--text)}footer{display:flex;justify-content:flex-end;gap:8px;margin-top:14px}button{border:1px solid var(--line);border-radius:6px;background:var(--surface-raised);padding:7px 10px;color:var(--text)}.primary{background:#1f6feb}</style>
